# Task Breakdown v2 — Round 2, Seat M (Implementability & Reference Accuracy)

- **Artifact under review:** `docs/responses-compat-apply-patch-task-breakdown.md` — v2, 545 lines, status "TASK BREAKDOWN — v2 (post round-1 review; resolution map in §9)"
- **SoT spec:** `docs/responses-compat-apply-patch-format.md` — v5, 1026 lines (locked; not re-reviewed)
- **Companion:** `docs/responses-compat-seam.md` (226 lines) · **v1 archive:** `docs/reviews/apply-patch-task-breakdown-v1-draft.md` (460 lines)
- **Branch / HEAD:** `feat/normalize-content-types-vllm` @ `197ea1642c7b2f4d417a07d2cea36ba5e7ec73d7` (verified `git branch --show-current` + `git rev-parse HEAD`)
- **Mandate:** implementability and reference accuracy of the v2 breakdown — Mandate 0 (17-row §9 resolution map), Mandate D (v1→v2 diff audit), Mandate 1 (full line-ref sweep at HEAD), Mandate 2 (per-item implementability), Mandate 3 (new-error check on v2-touched regions). Leaf reviewer, no subagents, read-only (only this report created; scratch under `/tmp/bd_r2_m`).
- **Date:** 2026-09-15 EDT · **Seat:** M (round 2, fresh)

# VERDICT

**APPROVED — 0 Blocking, 0 Major, 0 Minor, 4 Nit.**

The v2 breakdown is executable as written and reference-exact at HEAD. All 17 rows of
the §9 v1→v2 resolution map landed correctly; every one of the 15 diff hunks maps to a
declared §9 row with no off-map edits and no loss of v1-correct content; every one of
the ~50 distinct line citations is EXACT or an acceptable loose-endpoint range (OK);
every item's mechanisms (arm shape, test fns, scaffolding signatures, helpers, gate
recipes) were re-derived at source and confirmed executable. The four nits are
cosmetic endpoint/wording imprecisions, none of which could mislead an implementer or
cause rework.

# Method

- `sed -n` / `nl -ba | sed -n` / `grep -n` / `rg` / `diff -u` / `ls` / `find` / `python3`
  only; no cargo/just/git mutations; no subagents.
- Every claim in the breakdown, the §9 map, and (where needed to resolve ambiguity) the
  spec was re-derived at HEAD 197ea1642c. The R1 reports were used only as input to
  locate claims, never as evidence.
- Line-ref classification per mandate: EXACT (cited line is the asserted
  symbol/assertion) / OK (range: symbol inside, endpoints loose) / WRONG.
- The full v1→v2 diff (`diff -u v1-draft v2`) was hunk-by-hunk mapped to §9 rows;
  spec strings/counts were re-checked mechanically (python3 char/word counts of the
  §3.1 embedded texts).

# Mandate 0 — §9 v1→v2 resolution map (17 rows), re-derived at HEAD

| Row | Finding (abbrev) | Landed? | Evidence at HEAD |
|---|---|---|---|
| 1 | T2.3 insta flow → inline `assert_eq!` literal update | YES | `codex-rs/core/src/tools/handlers/apply_patch_spec_tests.rs`: fn `create_apply_patch_function_tool_matches_expected_spec` at :40, test :40-61, a single inline `assert_eq!` against a fully-literal `ResponsesApiTool` (no insta anywhere in the file — `grep insta` empty; no snapshots dir entry for it). Tool-description literal at :45, `patch` arg-description literal at :52 — both EXACT. v2 text (Item 2 step 5, overview row 2 "expected-literal update", DoD "expected literals", §9 row 1) all consistent; "no `cargo insta` step" stated. |
| 2 | Ledger #4 nonexistent `core/tests/suite/tool.rs` | YES | `find codex-rs/core/tests -name tool.rs` → empty (suite dir has `tools.rs`, no `tool.rs`). Corrected cite `codex-rs/apply-patch/tests/suite/tool.rs`:386 = fn `test_apply_patch_cli_rejects_invalid_hunk_header` (EXACT), assert (exact-stderr `.stderr(...)`) at :393 (EXACT). Both the ledger row 4 and the Item 3 overview files cell carry the corrected path. |
| 3 | "24 fixtures" → 25 | YES | `ls codex-rs/apply-patch/tests/fixtures/scenarios/` → 25 dirs + `README.md` (26 entries); two `020_*` dirs (`020_delete_file_success`, `020_whitespace_padded_patch_marker_lines`). Both v2 occurrences say 25 (§1 surviving-tests line; Item 1 T1.13). Runner iterates `fs::read_dir` dynamically (`tests/suite/scenarios.rs`:18) so the count cannot break the suite. |
| 4 | Item 1 step 6 `mod tests;` collision | YES | `streaming_parser.rs`:382 = `#[cfg(test)]`, :383 = `mod tests {` (EXACT; the only `mod tests` decl in the file). v2 step 6 declares `mod streaming_parser_p2_tests` with the collision reason stated; no `streaming_parser_p2_tests` exists anywhere in `codex-rs/` (no name collision); sibling-file convention exists in-crate (`file_update.rs`:15-17 uses the identical `#[cfg(test)] #[path = "..._tests.rs"]` pattern). §6 open question 1 (name confirmation) removed; remaining questions renumbered 1-3. |
| 5 | Item 4 step 2 helper body type-check | YES | `responses.rs`:933 `pub fn ev_function_call(call_id: &str, name: &str, arguments: &str) -> Value` (fn :933-943 EXACT). v2 helper body uses the sibling local-bind pattern of `ev_apply_patch_exec_command_call_via_heredoc` (:1030-1035 EXACT: `let args = serde_json::json!({…}); let arguments = serde_json::to_string(&args).expect(…); ev_function_call(call_id, "exec_command", &arguments)`). The v2 body (`let args = json!({"patch": patch}); let arguments = to_string(&args).expect(…); ev_function_call(call_id, "apply_patch", &arguments)`) type-checks: `&arguments` derefs `String`→`&str`; `Value` resolves (responses.rs:15 `use serde_json::Value`); `serde_json::json!`/`to_string` used fully qualified as in siblings; `ev_function_call` is same-module. E0308 note is correct Rust (owned `String` is not coerced to `&str` at an argument position). |
| 6 | Item 3 step 6 red-set progression | YES | Ownership from §1 ledger: Item 3 step 2 rewrites 12 (rows 1, 3-13). P3.1 (streaming :193 message) owns rows 1+4 → 12→10; P3.2 (:222) owns row 3 → 10→9; P3.3 owns no ledger row (new T3.1 tests only) → 9; P3.4 (parser.rs:268/:271) owns rows 5-13 → 0. "4 failing test fns" verified at HEAD: `test_streaming_patch_parser_returns_errors` (`streaming_parser.rs`:814; red asserts :828 & :848 — :838 belongs to Item 1 and is green by Item 3), `test_apply_patch_cli_rejects_invalid_hunk_header` (`tool.rs`:386, assert :393), `test_parse_patch` (`parser.rs`:277, asserts :281/:287), `test_parse_patch_lenient` (`parser.rs`:558, asserts :579/:594/:609/:624/:628/:635/:639-642). Mechanism for rows 1+4 sharing one fix verified: CLI path is `apply_patch_with_options` → `parse_patch` (`lib.rs`:370) → `parse_patch_text` (`parser.rs`:193) → boundary pre-pass + `StreamingPatchParser`; the hunk-header string exists ONLY in `streaming_parser.rs` (:193/:211/:222), and the CLI wraps it with the `lib.rs`:385 "Invalid patch hunk on line {n}: " prefix — so one edit at :193 turns both ledger #1 and #4 green. No other tests assert the changed strings (repo-wide grep: only `parser.rs` and `streaming_parser.rs`, all accounted for). |
| 7 | Item 3 T3.1 red-first + renumber | YES | v2 step 5: write both T3.1 tests → RED against the collapsed handler (`apply_patch.rs`:534-542; one `ok_or_else` at :537-541 with message :539 returns the same text for absent and non-string `patch` — re-derived) → land P3.3 → GREEN. Steps renumbered: 7 = T3.2, 8 = T1.14; old v1 step 7 (T3.1 description) deleted (diff-verified). Renumbering internally consistent (see Mandate 3). |
| 8 | §5 existence-check bullet false at source | YES | `lib.rs` AddFile apply branch :508-535: reads optional content for delta bookkeeping only (`read_optional_file_text_for_delta`, no `?`), then writes unconditionally via `try_write!(write_file_with_missing_parent_retry(…))` (:517-526) — no existence guard. DeleteFile apply branch :536-595: reads content :545-548, `ensure_not_directory` :552, propagates removal errors :560-587 (`return Err(error)` at :586). Fixture dirs `007_rejects_missing_file_delete`, `009_requires_existing_file_for_update`, `011_add_overwrites_existing_file` all exist. Core-suite test `apply_patch_cli_add_overwrites_existing_file` at `codex-rs/core/tests/suite/apply_patch_cli.rs`:668 (EXACT). v2 §5 bullet matches the spec §3.4 shape ("no existence check is ADDED on any path"). |
| 9 | `apply_patch.rs`:508-560 → :508-563 | YES | `handle_call` starts :508, closing `}` at :563 (EXACT). v2 cites :508-563 in Item 3 scope; no stale :508-560 remains outside §9's meta-text. |
| 10 | `registry.rs`:548-556 → :549-565 | YES | `if !tool.matches_kind(&invocation.payload) {` at :549 (EXACT); message `format!("tool {tool_name} invoked with incompatible payload")` at :550 (EXACT); block-closing `}` at :565 (EXACT). v2 Item 4 step 3 cites :549-565 + :550. |
| 11 | `responses.rs`:39-58 → :39-60 | YES | `struct ResponseMock` :39-41, `single_request` :50-56, `requests` :58-60 — all EXACT. v2 Item 4 step 1 cites :39-60. |
| 12 | §8 execution-log row reaches commit outside review loop | YES | §4 step 6 now contains the coordinator-bookkeeping declaration (exempt from the item code-review loop; factual pointers only to already-reviewed `docs/reviews/impl-<item>-r<N>.md`; row lands in the item's own commit; commit-SHA cell by a follow-up exempt edit). §8 table shape unchanged: columns item / red evidence / green evidence / review rounds (0B+0M at) / commit, rows 1-6 — consistent with the declaration (review-round pointers exist at commit time; SHA cannot, hence the follow-up edit). |
| 13 | Spec §1.2 "≈60×" vs R5 map "≈58×" | YES (deferred, as declared) | Spec §1.2 conclusion (line 209) still reads "≈60× qwen's ~1% (26/37 = 70.27% vs 5/413 = 1.21%)"; spec §7 R5 map (line 1018) records "(70.27% / 1.21% ≈ 58×)". Exact ratio (26/37)/(5/413) = 10738/185 = 58.04×. v2 Item 5 step 2 defers the "≈60×" → "≈58×" correction to the spec final pass; the v2 breakdown itself leaves the spec untouched (verified — spec still says ≈60×). |
| 14 | L's supporting-table cite `lib.rs:374-384` | YES (no breakdown fix, as declared) | The breakdown's own cite is `lib.rs`:370 = `let hunks = match parse_patch(patch) {` in `apply_patch_with_options` (EXACT at HEAD). The :374-384 cite appears only in §9 row 14 as a reference to seat L's internal table. |
| 15 | Status line + review log | YES | Line 9: "Status: TASK BREAKDOWN — v2 (post round-1 review; resolution map in §9)". §9 present (round-1 table with both R1 reports + corroboration notes + 17-row map). R1 report files exist (`r1-seatL.md` 593 lines, `r1-seatK.md` 740 lines). |
| 16 | §0 bullet "round-4/5 seats: 0 wrong" stale | YES | §0 bullet (lines 34-38) now reads: refs "re-verified at HEAD by the round-1 breakdown seats (full citation sweeps in both R1 reports); every citation found wrong or off in round 1 was corrected in this v2 revision (§9, rows 2, 3, 9, 10, 11)". Accurate — my sweep found exactly those corrected cites, all now correct (plus the loose-endpoint nits listed in Findings, none of which are wrong). |
| 17 | v1 archival | YES | `docs/reviews/apply-patch-task-breakdown-v1-draft.md` exists (460 lines); header line 7 carries the archive pointer ("v1 archive: `docs/reviews/apply-patch-task-breakdown-v1-draft.md`"). |

**Result: 17/17 rows landed correctly. No row wrong at HEAD.**

# Mandate D — v1→v2 diff audit (15 hunks)

`diff -u docs/reviews/apply-patch-task-breakdown-v1-draft.md docs/responses-compat-apply-patch-task-breakdown.md` → 15 hunks. Every hunk maps to one or more declared §9 rows; no change outside declared regions; no loss of v1-correct content (checked each replaced passage for surviving required sentences).

| Hunk | v1→v2 region | Maps to §9 row(s) | Audit |
|---|---|---|---|
| `@@ -4,11 +4,11` | Header: archive pointer added; status "drafted (stage 3)" → "v2 (post round-1 review; resolution map in §9)" | 15, 17 | Clean. "No implementation until … zero Blocking and zero Major" sentence preserved verbatim. |
| `@@ -31,9 +31,11` | §0 line-ref bullet rewritten | 16 | Clean. "They may drift … re-verify its own references before editing" tail preserved. New claim (R1 re-verification; rows 2, 3, 9, 10, 11 corrected) verified true by my sweep. |
| `@@ -45,7 +47,7` | Ledger row 4 path `codex-rs/core/tests/suite/tool.rs` → `codex-rs/apply-patch/tests/suite/tool.rs` | 2 | Clean. Rest of the row (fn :386, assert :393, expectation text, owning item) untouched. |
| `@@ -55,7 +57,7` | §1 "24 golden scenario fixtures" → "25" | 3 | Clean. Single token change; both cite locations now 25. |
| `@@ -69,8 +71,8` | Overview row 2: "+ snapshot update" → "+ expected-literal update"; files cell drops "(+ snapshot)". Overview row 3: `tests/suite/tool.rs` → full corrected path | 1, 2 | Clean. All other table cells untouched. |
| `@@ -121,13 +123,16` | Item 1: T1.13 "24" → "25"; step 6 rewritten (module name `streaming_parser_p2_tests` + collision rationale + inline-mod cite :382-383) | 3, 4 | Clean. "The rewritten assertion :838 stays in the existing inline module" sentence preserved. New cite EXACT (verified). |
| `@@ -169,19 +174,21` | Item 2 T2.3: "full-snapshot test / cargo insta flow" → "spec-equality test / inline assert_eq / update literals :45/:52 / no cargo insta step"; DoD "snapshot updated" → "expected literals updated" | 1 | Clean. "Intentional change, not a regression — say so in the commit message" preserved verbatim; "green" outcome preserved. New cites EXACT. |
| `@@ -195,7 +202,7` | Item 3 P3.3 bullet: `:508-560` → `:508-563` | 9 | Clean. Single range change; EXACT at HEAD. |
| `@@ -220,19 +227,26` | Item 3 steps: step 5 red-first T3.1 (absorbs old step 7); step 6 gate-boundary sentence → red-set progression (12→10→9→9→0, 4 fns); steps renumbered (T3.2 → 7, T1.14 → 8) | 6, 7 | Clean. T3.1 content (both message shapes, `apply_patch_tests.rs` location) fully preserved in new step 5. T3.2 step text unchanged except number. "T1.14 is satisfied by steps 2+6" still points at the correct v2 steps (2 = 12-assertion rewrite incl. parser.rs rows 5-13; 6 = P3.4 lands) — re-verified. |
| `@@ -253,18 +267,30` | Item 4 step 1: `responses.rs`:39-58 → :39-60. Step 2: helper signature line → full code block + E0308 note; old one-line form retained as the "does not compile" counterexample | 5, 11 | Clean. Sibling-helper cites (:1025-1028, :1030-1035, :933-943) unchanged and EXACT. New code block compiles (verified). New range EXACT. |
| `@@ -272,7 +298,7` | Item 4 step 3: `registry.rs`:548-556 → :549-565 | 10 | Clean. "message :550" preserved; both new endpoints EXACT. |
| `@@ -315,8 +341,11` | Item 5 step 2: adds the ≈60× → ≈58× correction with K-N1 rationale; "leave the §7 review log and all archives untouched" → "… otherwise untouched" | 13 | Clean. All other step-2 content (status line, five SHAs) preserved. |
| `@@ -385,6 +414,12` | §4 step 6: appends the §8-bookkeeping declaration (K-N3) | 12 | Clean. Pure addition after the existing "Update this breakdown's §8 execution log, then start the next item." sentence; no other §4 steps touched. |
| `@@ -418,24 +453,28` | §5 existence-check bullet rewritten to spec §3.4 shape; §6 open question 1 (module-name confirmation) deleted; questions 2-4 renumbered 1-3 | 8, 4 | Clean. "No Update-File leniency; no `strict:true`; no client-side retry of `{}` calls; no per-model parser modes" tail of the bullet preserved verbatim. Remaining open questions' text unchanged (only numbering). New cites all verified EXACT/OK (see Findings for two endpoint nits). |
| `@@ -458,3 +497,49` | Appends §9 (round-1 table, corroboration notes, 17-row map, round-2 termination rule) | 15 (bookkeeping) | Clean. Pure addition; §8 table tail appears only as context. |

**Sentence-flow / numbering / cross-reference checks around each hunk:** Item 3 steps now run 1-8 with no gaps or duplicates; its step 8 "steps 2+6" pointer verified against the new numbering. §6 open questions renumbered 1-3, each still matching its item (Q1 → Item 3 step 3; Q2 → Item 4 step 3; Q3 → Item 6 sub-step 2). DoD/Gates lines read consistently with the rewritten steps (Item 2 DoD "expected literals"; Item 3 DoD references the same surviving tests as §1). No dangling "see below" pointers (the only forward reference, v1's step 5 "see below" for T3.1, was removed with the renumber). The "§8 bookkeeping" declaration in §4 step 6 is consistent with §8's table shape (columns item/red/green/review rounds/commit).

**Result: 15/15 hunks map to declared §9 rows; zero off-map edits; zero loss of v1-correct content.**

# Mandate 1 — Full line-ref sweep at HEAD

Method: regex-extracted every `file:NNN` / `file:NNN-MMM` / bare `:NNN` citation from the v2 breakdown, then re-read §1, the item sections, and §9 to catch bare refs. Each location opened at HEAD with `nl -ba | sed -n`. Citations that appear ONLY in §9's meta-text describing old v1 values or another document (`:39-58`, `:548-556`, `:508-560`, `lib.rs:374-384`) are marked META (not claims about v2's state; verified as accurate descriptions of v1/L's report).

| File (at HEAD) | Cites in v2 | Status |
|---|---|---|
| `codex-rs/apply-patch/src/streaming_parser.rs` | :193 (P3.1 msg) EXACT · :198-214 (AddFile arm) OK (arm closes :215) · :199 (header check) EXACT · :202-208 (`+` branch) EXACT · :209-214 (trailing Err) EXACT · :222 (P3.2 msg) EXACT · :168/:184/:374 (diff-consumer msgs, unchanged) EXACT×3 · :382-383 (inline `#[cfg(test)] mod tests {`) EXACT · :784, :797, :819 (surviving asserts) EXACT×3 · :814 (`test_streaming_patch_parser_returns_errors` fn) EXACT · :828, :838, :848 (ledger asserts 1/2/3) EXACT×3 | **All EXACT except arm range OK** |
| `codex-rs/apply-patch/src/parser.rs` | :57 (`InvalidPatchError` Display "invalid patch: {0}") EXACT · :193-199 (`parse_patch_text` pre-pass) OK (fn :193-211; pre-pass :195-198; start EXACT) · :256-274 (`check_start_and_end_lines_strict`) EXACT · :268 (Begin msg) EXACT · :271 (End msg) EXACT · :277 (`test_parse_patch` fn) EXACT · :281/:287 (asserts) EXACT×2 · :558 (`test_parse_patch_lenient` fn) EXACT · :579/:594/:609/:624/:628/:635 (6 Begin asserts) EXACT×6 · :639-642 (End assert) EXACT | **All EXACT except :193-199 OK (M-N1)** |
| `codex-rs/apply-patch/tests/suite/tool.rs` | :386 (fn) EXACT · :393 (exact-stderr assert) EXACT | **EXACT** |
| `codex-rs/apply-patch/src/lib.rs` | :370 (CLI `parse_patch` call in `apply_patch_with_options`) EXACT · :508-534 (AddFile apply branch) OK (branch closes :535; "writes unconditionally" content :517-526 inside) · :536-595 (DeleteFile apply branch) EXACT · :545-548 (content read) EXACT · :560-586 (removal-error propagation) OK (if-let closes :587; `return Err(error)` at :586 inside) · :374-384 META (§9 row 14, L's table) | **EXACT except two endpoint-OK ranges (M-N2, M-N3)** |
| `codex-rs/apply-patch/src/invocation.rs` | :116/:123/:170/:175 (shell-intercept `parse_patch` calls) EXACT×4 | **EXACT** |
| `codex-rs/core/src/tools/handlers/apply_patch.rs` | :508-563 (`handle_call`, closing brace :563) EXACT · :534-536 (collapsed `get("patch").and_then(Value::as_str)`) OK (full expr :534-542; `ok_or_else` elided as "…") · :539 (collapsed message) EXACT · :603-605 (`matches_kind`, `ToolPayload::Function` only) EXACT · :534-541 (overview cell) OK (expr ends :542) · :508-560 META (§9 row 9) · :411 (spec cite, function-handler `parse_patch` call) — re-verified EXACT for spec consistency | **EXACT except two OK ranges** |
| `codex-rs/core/src/tools/registry.rs` | :549-565 (kind-mismatch block) EXACT · :549 (`if !tool.matches_kind`) EXACT · :550 (message) EXACT · :548-556 META (§9 row 10) | **EXACT** |
| `codex-rs/core/tests/common/responses.rs` | :39-60 (struct + `single_request` + `requests`) EXACT (endpoints exact: struct start :39, `requests` end :60) · :39-41 / :50-56 / :58-60 EXACT×3 · :933 (`ev_function_call`) EXACT · :933-943 (fn) EXACT · :1025-1028 (`ev_exec_command_call_with_args`) EXACT · :1030-1035 (`ev_apply_patch_exec_command_call_via_heredoc`) EXACT · :39-58 META (§9 row 11) | **EXACT** |
| `codex-rs/core/tests/common/test_codex.rs` | :347 (`with_config`) EXACT · :839-859 (built-in "openai" provider clone block: construction :839-845, `config.model_provider = …` :859) OK | **EXACT / OK** |
| `codex-rs/core/tests/suite/apply_patch_cli.rs` | :228 (`mount_apply_patch`) EXACT · :323 (`read_file_text` usage) EXACT · :707 (substring assert `out.contains("is not a valid hunk header")`) EXACT · :668 (`apply_patch_cli_add_overwrites_existing_file` fn; §9 row 8) EXACT | **EXACT** |
| `codex-rs/core/src/tools/handlers/apply_patch_spec_tests.rs` | :40-61 (test fn, closing brace :61) EXACT · :45 (tool description literal) EXACT · :52 (`patch` arg description literal) EXACT | **EXACT** |
| `codex-rs/core/src/tools/handlers/apply_patch_spec.rs` | `create_apply_patch_function_tool` (file-level; fn at :40) + freeform counterpart :13 | **EXACT** |
| `codex-rs/core/src/tools/handlers/apply_patch_tests.rs` | :45 (`invocation_for_payload`) EXACT | **EXACT** |
| `codex-rs/core/src/session/tests.rs` | :5882 (`make_session_and_context`) EXACT | **EXACT** |
| `codex-rs/core/src/tools/spec_plan.rs` | :1257-1271 (capability gate: Function vs freeform handler selection) EXACT | **EXACT** |
| `codex-rs/model-provider/src/provider.rs` | :372 (`apply_patch_function_tool: !self.info.is_openai()`) EXACT | **EXACT** |
| `codex-rs/model-provider-info/src/lib.rs` | :546-547 (`is_openai`, name match only) EXACT · :40 (`OPENAI_PROVIDER_NAME = "OpenAI"`) EXACT | **EXACT** |

**Every reference new or changed in v2 (§9 rows 1-11): `apply_patch_spec_tests.rs`:40-61/:45/:52, `apply-patch/tests/suite/tool.rs`:386/:393, `streaming_parser.rs`:382-383, `responses.rs`:933/:1030-1035 (helper context), `responses.rs`:39-60, `apply_patch.rs`:508-563, `registry.rs`:549-565/:549/:550, `lib.rs`:508-534/:536-595/:545-548/:560-586 (row 8), fixture dirs 007/009/011, core-suite :668, `lib.rs`:370 (row 14) — all EXACT or OK. Zero WRONG.**

File-level refs: spec (1026 lines), seam doc (226 lines), v1 archive (460 lines), both R1 reports, scenarios dir (25 dirs + README.md), justfile recipes (`test`/`fix`/`fmt`/`bazel-lock-update` all exist; `set working-directory := "codex-rs"` explains the codex-rs-relative invocations), `codex-model-provider` package, `apply_patch` bin target (Cargo.toml :12-14) — all verified present at HEAD.

# Mandate 2 — Per-item implementability walkthrough

**Item 1 (P2, Add-File leniency).** Scope arm shape re-derived: `StreamingParserMode::AddFile` arm :198-215 with header check :199 (`handle_hunk_headers_and_end_patch(trimmed)`), `+` branch :202-208 (strip prefix, append content + `\n`), trailing `Err(InvalidHunkError)` :209-214 — the spec §3.2 change (verbatim-append branch + remove unreachable Err) fits inside the arm, ~8 lines net, as claimed. Step 2's RED is genuine: at HEAD the `bad`-line-in-Add-File case errors (assert :835-842); rewritten to `Ok(vec![AddFile { contents: "bad\n" }])` it fails. All step-5 sub-cases are mechanically grounded: structural lines handled by `handle_hunk_headers_and_end_patch` (:84-137 — `*** End Patch` exact-on-trim → structural, real headers structural in all modes, `*** Environment ID:` only in StartedPatch, typo'd `*** Ad File:` falls through to content), `+*** End Patch` takes the `+` branch (structural check sees the trimmed line) so it stays content — matches T1.7/T1.9b exactly. T1.13 golden scope: the runner (`tests/suite/scenarios.rs`:13-19) iterates `read_dir` over the 25 fixture dirs dynamically and asserts per-scenario exit/output — "pass unmodified" is a well-defined gate. Step 6 module declaration is legal Rust in `streaming_parser.rs`'s context: exactly one `mod tests` exists (inline, :383); `streaming_parser_p2_tests` is unique repo-wide; the declaration mirrors the in-crate sibling convention (`file_update.rs`:15-17) and AGENTS.md's `#[path = "..._tests.rs"]` rule; the new file lands at `src/streaming_parser_p2_tests.rs` (path relative to the declaring file's dir). Gates `just test -p codex-apply-patch` / `just fix -p codex-apply-patch` are real recipes (root justfile `test *args` → `cargo nextest run --no-fail-fast`, `fix *args` → `cargo clippy --fix --tests --allow-dirty`; `set working-directory := "codex-rs"`). DoD consistent. **Executable as written.**

**Item 2 (P1, function-tool spec text).** `create_apply_patch_function_tool` exists (`apply_patch_spec.rs`:40) with the current 2-sentence arg description; the spec §3.1 embedded texts were re-counted mechanically (python3): tool description 126 chars/21 words, arg description 2,198 chars/380 words, total 2,324/401 — exactly as the breakdown states; all 7 drift substrings present in the arg text; the T2.2 example remainder (first line exactly `Example:` → end `*** End Patch`) contains the claimed hunks (Add `notes/todo.md` = `# TODO\n\n1. ship the fix\n`; Update `src/main.rs` = one chunk, context `fn main`, 1 removal / 1 addition / 1 context) — matches spec §3.1/T2.2 verbatim. Capability gate re-verified: `spec_plan.rs`:1257-1271 selects `FunctionApplyPatchHandler` iff `capabilities().apply_patch_function_tool`, and `provider.rs`:372 sets it `!self.info.is_openai()` — so "non-OpenAI only, no gate change" is source-true. T2.1 RED is genuine (none of the 7 substrings occur in the current text). T2.2 is implementable from core: `codex_apply_patch::parse_patch`/`Hunk`/`UpdateFileChunk` (PartialEq) are re-exported at the crate root (`apply-patch/src/lib.rs`:24-28) and codex-core already imports the crate (`apply_patch.rs`:411). T2.3: the two literals at :45/:52 are the only description-bearing fields in the fully-literal expected `ResponsesApiTool`; the sibling env-id test (:64) checks properties only and is unaffected. T2.4: the two freeform tests (:5, :24) exist. Gates real. **Executable as written.**

**Item 3 (P3, teachable errors).** All four sites re-derived (Mandate 0 rows 6-9). Step 2's "all 12 RED" holds: each of the 12 rewritten assertions has a new expectation that differs from HEAD behavior (P3.1 extension at :193 affects rows 1+4; P3.2 replacement at :222 affects row 3; P3.4 rewrites of parser.rs:268/:271 affect rows 5-13 — the six lenient Begin asserts share the `expected_error` local at :575-576, so one literal rewrite + the :642 End literal covers all seven; the cited assert lines are all exact). No collateral asserts in those four fns depend on the changed strings (verified the remaining assertions in each fn against the unchanged message sites :184, :270-area Update messages, Ok cases). Step 3's substring check is consistent: P3.1 keeps the old sentence verbatim as a prefix, so `apply_patch_cli.rs`:707's `contains("is not a valid hunk header")` stays green (the core-suite test's patch is the StartedPatch case — re-verified at :689-710). Step 5's RED-first T3.1 is grounded: the collapsed `ok_or_else` (:537-541, message :539) returns the same text for absent and non-string `patch`, and both new §3.3.3a/3b strings differ from it; the test can reach the handler in-crate via the public `ToolExecutor::handle` (trait impl :499-504 wraps the private `handle_call`) with payloads built the way existing tests do (`ToolPayload::Function { arguments }` + `invocation_for_payload` :45); `FunctionApplyPatchHandler::new` is `pub(crate)`. Step 6 progression arithmetic re-derived (12→10→9→9→0; 4 fns) — and the mid-item gates stay satisfiable: all 12 ledger asserts live in the `codex-apply-patch` crate, so `just test -p codex-core apply_patch` (which covers the surviving `:707` substring test and the core apply_patch suites) is green throughout the window while the apply-patch-crate run shows exactly the declared red set. Step 7 T3.2: scaffolding exists (`invocation_for_payload` :45 → `make_session_and_context` `session/tests.rs`:5882); the raw Add-File patch parses once Item 1's P2 is landed and executes against the session's sandboxed FS (LOCAL_FS available in that test module) — green at write time. Step 8's "steps 2+6" pointer correct under the v2 numbering. DoD/Gates consistent. **Executable as written.**

**Item 4 (T4, integration).** Step 1 T4.1: the default harness provider is the built-in "openai" clone (`test_codex.rs`:839-845, name = `OPENAI_PROVIDER_NAME` "OpenAI") → freeform path at HEAD; `with_config` (:347) can rename `config.model_provider.name` → capability flips → function tool on the wire; the captured `/v1/responses` body is assertable via `ResponseMock::single_request()`/`requests()` (:50-56/:58-60) which hold the full `ResponsesRequest` (body_json per repo conventions). Green-at-write regression lock over Items 1-2 — correct characterization. Step 2 helper compiles in `responses.rs` (Mandate 0 row 5). Step 3: `mount_apply_patch` (:228) indeed hardcodes `ev_apply_patch_custom_tool_call` (:240) and is unusable for the function path; `apply_patch_responses` (:266-283) takes `apply_patch_call: fn(&str, &str) -> serde_json::Value` — the new helper matches that signature exactly, so the "function-call variant" is a one-argument change; the registry rejection of the wrong kind is real (`registry.rs`:549-565, `matches_kind` :603-605). Steps 4/5 (T4.2/T4.2b): `read_file_text` harness method used at :323; overwrite behavior grounded in `lib.rs`:508-535. Step 6 conventions (`wait_for_event`/`mount_sse_once`) per AGENTS.md. Gates real. DoD "3 tests + helpers, no product code" consistent. **Executable as written.**

**Item 5 (docs/gates/spec final pass).** Seam doc verified to have every target section: §2 line 54 contains the stale "plus an Azure branch" sentence (Item 5's correction target exists; `is_openai` at `model-provider-info/src/lib.rs`:546-547 is a pure name match — no Azure branch at source); §3 (line 58) for the P1/P2/P3 layer; §4 divergence map (line 139); §6 conflict sites (line 185) and verification gate (line 197); invariants (line 220). Evidence numbers (26/37 ≈ 70%, 13/21 ≈ 62%, 5/413 ≈ 1%) match spec §1.2/§6. Spec final pass: the "≈60×" string exists at spec line 209 and the correction target is unique; the five-SHA status update is well-defined. Full gates all real recipes, including `just test -p codex-model-provider` (package name verified in `codex-rs/model-provider/Cargo.toml`) and `just bazel-lock-update` (justfile :144). **Executable as written.**

**Item 6 (real-env + bead close).** Operational, post-commit, operator-in-the-loop. The standalone `apply_patch` binary exists (`[[bin]] name = "apply_patch"`, `apply-patch/Cargo.toml`:12-14, `main.rs` → `codex_apply_patch::main()`) and shares `parse_patch` with the function handler (verified call chain) — the raw-format probe is a pinned, deterministic mechanism as claimed. All seven sub-steps map 1:1 to spec §5 steps 1-7 plus the bead close; the bead command (`command bd -C /Users/palanisd/Projects/bitbucket/apex_tracking close apex-ayl.52`) matches the AGENTS.md APEX campaign-state convention. Wiretap path is operator-environment (not verifiable in-repo; flagged as operational, consistent with spec). **Executable as written (operational).**

# Mandate 3 — New-error check on v2-touched regions

- **Item 1 step 6 (module declaration):** no duplicate module name (only `mod tests` exists at :383; new name unique repo-wide); sibling-file `#[path]` convention matches AGENTS.md's test-module rule and the in-crate precedent (`file_update.rs`:15-17); consistent with Item 1 DoD ("git diff limited to the AddFile arm + test files"). No contradiction with the rest of the breakdown or AGENTS.md.
- **Item 2 T2.3 ("no cargo insta step"):** aligns with AGENTS.md's snapshot policy — that policy prescribes the insta flow for snapshot tests; this test has no insta snapshot (verified: no `insta` in the file, plain `assert_eq!`), so no insta step is required and none is prescribed. The rewrite "update the two expected literals … intentional change, not a regression — say so in the commit message" is consistent with spec T2.3 ("updated to the new exact text — an intentional change, not a regression"). No protocol contradiction.
- **Item 3 steps 5-8:** red-first T3.1 ordering is consistent with §4 protocol step 2 (failing test → RED → implement → GREEN); the step 6 progression is satisfiable (verified arithmetic + the core-crate-green-during-window property); renumbering is internally consistent (steps 1-8, no gaps; step 8's "steps 2+6" pointer correct; §9 row 7's "8→7 (T3.2), 9→8 (T1.14)" matches the diff). §4 step 2's generic "run the item gates" is not contradicted: item 3 step 6 specifies what "run" means inside the red window (permitted failures = the declared red set only), and full-green gates are required before review/commit — the item-specific refinement governs.
- **Item 4 steps 1-3:** the "default provider is named 'OpenAI' (→ freeform path)" claim is source-true (built-in clone + `is_openai` name match + capability at `provider.rs`:372); the E0308 explanation is correct Rust; the helper's `pub fn` in `tests/common/responses.rs` matches the file's existing `pub fn` helper convention and is consumed by the new mount helper (not a single-reference helper, so no AGENTS.md helper-proliferation violation); the `apply_patch_responses` function-pointer option type-checks (signature `fn(&str, &str) -> serde_json::Value` matches the helper exactly, `Value` = `serde_json::Value`).
- **Item 5 step 2 (≈60× → ≈58×):** the correction target and value are verified (spec line 209; 58.04× exact from pinned counts; R5 map records ≈58×). Editing the spec is declared inside the implementation plan (Item 5 = T5 "spec final pass"), not by this review round — the v2 breakdown itself leaves the locked spec untouched (verified). No contradiction with the spec's locked status.
- **§4 step 6 / §8 bookkeeping:** declaration consistent with §8's table shape (item / red / green / review rounds / commit) and with §4 step 5 (one commit per item): the row's red/green + review-round pointers exist at commit time; only the SHA cell needs the follow-up exempt edit. The declaration's scope ("factual pointers only to the already-reviewed `docs/reviews/impl-<item>-r<N>.md` records") matches §4 step 4's record path.
- **§5 bullet (existence check):** matches source (`lib.rs`:508-595) and spec §3.4/§3.2 decision wording; scenario pins 007/009/011 exist; the core-suite overwrite test exists at :668. No contradiction.
- **§6 open questions (renumbered 1-3):** each question's target item/step exists and is unchanged in substance; the removed question 1 is genuinely resolved by Item 1 step 6's stated module name.
- **§9 map text vs reality:** every sub-claim in rows 1-17 re-verified in Mandate 0 (all true at HEAD); row 3's "both occurrences now say 25" confirmed; row 4's "§6 open question 1 removed" confirmed in the diff.

No v2-touched wording contradicts the rest of the breakdown, the spec, or AGENTS.md.

# Findings

No Blocking, no Major, no Minor. Four nits:

**M-N1** — Nit
- **Where:** Item 3 scope (P3.4 bullet): "`parse_patch_text` (`parser.rs`:193-199)".
- **Evidence:** `parse_patch_text` spans :193-211 at HEAD; the boundary pre-pass (the surface P3.4 touches via this fn) is :195-198; the range ends at the blank line :199, i.e. the endpoint is loose and the fn's streaming-parse portion (:200-211) is outside the cited range.
- **Impact:** None practical — the fn name is cited and the pre-pass is clearly the subject; an implementer opening `parser.rs`:193-199 sees exactly the pre-pass. Cosmetic imprecision only. Pre-existing from v1 (not a v2-introduced change).
- **Suggested resolution:** Optional at v3 or during Item 3 execution: cite as `parse_patch_text` (`parser.rs`:193; pre-pass :195-198).

**M-N2** — Nit
- **Where:** §5 invariant bullet and §9 row 8: "the AddFile apply branch `lib.rs`:508-534 writes unconditionally".
- **Evidence:** The `Hunk::AddFile` arm spans :508-535 at HEAD (closing brace :535); the cited range ends one line early, at `added.push(affected_path);` (:534). In the same sentence the DeleteFile branch is cited :536-595, which includes its closing brace — the two endpoints are one line inconsistent.
- **Impact:** None — all content the sentence relies on (the unconditional `try_write!` at :517-526) is inside the range. Cosmetic off-by-one at the endpoint, v2-introduced wording (§9 row 8 / §5 bullet), but not misleading.
- **Suggested resolution:** Optional: `lib.rs`:508-535.

**M-N3** — Nit
- **Where:** §5 invariant bullet and §9 row 8: "the DeleteFile apply branch `lib.rs`:536-595 reads the file's content and propagates removal errors" (sub-detail :560-586 in §9 row 8's evidence).
- **Evidence:** The removal-error if-let spans :560-587 (closing brace :587; `return Err(error)` at :586). The :560-586 endpoint is one line short of the brace, same off-by-one class as M-N2.
- **Impact:** None — the propagated-error content is fully inside the range.
- **Suggested resolution:** Optional: :560-587.

**M-N4** — Nit
- **Where:** §9 row 3 resolution text: "fixture dirs 001-024 include two `020_*` dirs plus `README.md`".
- **Evidence:** `README.md` is a file, not a fixture dir, and is not part of the 25; the phrasing attaches it to the dir set ("plus README.md" after "include two `020_*` dirs"). The substantive claim — 25 fixture dirs at HEAD, runner counts dynamically — is correct and both breakdown occurrences say 25.
- **Impact:** Meta-text readability only; the count and the dynamic-runner fact (the load-bearing parts) are accurate.
- **Suggested resolution:** Optional rewording, e.g. "25 fixture dirs (numbered 001-024 with two `020_*` dirs; `README.md` alongside)".

# Re-verified clean (not findings)

- All 13 ledger assertions located at their exact cited lines; red-set complete (repo-wide grep found no other test asserting any changed string); "4 failing test fns" exact.
- P3.1 one-fix-two-green mechanism (streaming :193 + CLI via `lib.rs`:370→`parse_patch`→streaming parser, prefix at `lib.rs`:385) — source-true.
- Item 3 red-window gates satisfiable: all 12 ledger asserts live in `codex-apply-patch`; `just test -p codex-core apply_patch` stays green throughout the window (surviving `:707` substring preserved by the sentence-extension prefix).
- T3.1 RED-first grounding: collapsed `ok_or_else` returns one message for both absent and non-string `patch`; both §3.3.3a/3b strings differ; in-crate test path via `ToolExecutor::handle` exists.
- Item 4: `mount_apply_patch` hardcodes the custom-tool event (unusable as warned); `apply_patch_responses`' fn-pointer parameter accepts the new helper verbatim; helper compiles in `responses.rs`'s import context; `ResponseMock` capture surface (:39-60) supports the T4.1 wire assertion.
- Spec §3.1 texts: 126 + 2,198 = 2,324 chars, 21 + 380 = 401 words (mechanically re-counted); 7 drift substrings all present; T2.2 example hunks match.
- Spec §3.3 message strings (4 sites, including §3.3.4 Begin/End full texts) and §3.4 invariants match the breakdown's per-site descriptions (extension/replacement/split/pre-pass).
- Spec §4 T-plan: all T-references used by the breakdown (T1.1-14 incl. 9b, T2.1-4, T3.1-2, T4.1/4.2/4.2b, T5) exist and are covered exactly once across Items 1-5; spec §5/§6 content matches Item 6 / Item 5 bullet-for-bullet.
- Evidence numbers: 26/37 (70.27%), 13/21 (~62%), 5/413 (1.21%); 58.04× = (26·413)/(37·5) exact.
- Capability gate (`spec_plan.rs`:1257-1271, `provider.rs`:372), `is_openai` name-match + constant (`model-provider-info/src/lib.rs`:546-547, :40), seam doc's stale "plus an Azure branch" sentence (line 54) and all §2/§3/§4/§6 update targets — all present as described.
- Justfile: `test *args` (nextest), `fix *args` (clippy --fix), `fmt`, `bazel-lock-update` exist; `set working-directory := "codex-rs"` makes every gate invocation in the breakdown well-formed; `codex-model-provider` package and `apply_patch` bin target exist.
- Structural-line semantics for P2 (T1.7/T1.8/T1.9b) re-derived from `handle_hunk_headers_and_end_patch` (:84-137): End-Patch exact-on-trim structural, real headers structural, Environment-ID only in StartedPatch, typo'd markers fall through to content, `+*** End Patch` takes the `+` branch.
- v1→v2 diff: 15/15 hunks on-map; no v1-correct sentence lost; numbering/pointers consistent after renumber (Item 3 steps 1-8, §6 questions 1-3).
- Bead identity (`apex-ayl.52`) and close command match AGENTS.md APEX campaign-state conventions; spec §6 names the same bead.

# Counts

| Severity | Count | IDs |
|---|---|---|
| Blocking | 0 | — |
| Major | 0 | — |
| Minor | 0 | — |
| Nit | 4 | M-N1, M-N2, M-N3, M-N4 |
| **Total** | **4** | |

**Verdict: APPROVED — 0 Blocking, 0 Major** (4 nits recorded, non-blocking per campaign convention).
