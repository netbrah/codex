# Item 5 — Docs, gates, spec final pass: review round 2, SEAT D (independent re-review + r1 cross-check)

- **Bead:** `apex-ayl.52` · **Branch:** `feat/normalize-content-types-vllm` @ HEAD `799ec98053`
- **Date:** 2026-09-16 · **Seat:** D (fully independent — reviewed from scratch; r1 reports read only in the §E cross-check)
- **Mode:** static source verification only (no `cargo`/`just` runs, per task constraint); read-only on all artifacts; no git write operations. Raw worker gate logs in `/tmp/item5-gate{1..5}.log` re-audited statically (files survive).
- **Artifacts under review (all untracked):** `docs/responses-compat-seam.md` (294 lines), `docs/reviews/impl-item5-tdd-evidence.md` (550 lines), `docs/responses-compat-apply-patch-format.md` (1029 lines)
- **SoT:** spec v5 (`docs/responses-compat-apply-patch-format.md` §1.2, §3.1–§3.4, §5.4, §6, §7), breakdown v3 (`docs/responses-compat-apply-patch-task-breakdown.md` "Item 5", §8), coordinator task contract for Item 5

## Method

- Read the breakdown Item 5 scope block (line 327) and spec §6 change list first; verified every seam-doc addition against that list, then against source.
- Grep/line-verified every code citation in the seam doc at the current tree; byte-compared the P1 spec text (spec §3.1 blocks) against the `apply_patch_spec.rs` constants programmatically; compared the P3 error strings and the §3.4 invariant text verbatim against code/spec.
- Re-derived all arithmetic independently (26/37, 5/413, 13/21, exact ratio, rounded-division ratio).
- Diffed the spec against the v4 archive (`docs/reviews/apply-patch-format-spec-v4-draft.md`); classified every hunk.
- Re-audited the worker's raw gate logs (summary lines, binding grep, panic census, per-target test-ID populations) and re-verified commit SHAs/dates/change-sets via `git show --stat`.
- Last: read `docs/reviews/impl-item5-r1-seatA.md` + `impl-item5-r1-seatB.md` and cross-checked each resolution in the evidence doc's "Round-1 findings and resolutions" log against the artifact text.

## A. Source verification (seam doc vs tree)

**A1. §2 `is_openai()` correction — VERIFIED.** `codex-rs/model-provider-info/src/lib.rs:40` = `const OPENAI_PROVIDER_NAME: &str = "OpenAI";` (single hit); `:546-547` = `pub fn is_openai(&self) -> bool {` / `self.name == OPENAI_PROVIDER_NAME` (single hits) — exact, case-sensitive `&str ==`; no Azure branch (`grep -in azure` over the file: zero matches). Azure-*named* providers therefore take the function-tool path — pinned by the capability test `configured_provider_apply_patch_function_tool_matches_provider_support` (`codex-rs/model-provider/src/provider.rs:707-733`): OpenAI → `false`, `name: "Azure"` → `true`, custom → `true` (exactly the three cases the invariants bullet claims). Seam doc cites `:546-547` and `:40` — both real.

**A2. §2 capability table — VERIFIED.**
- `normalize_content_types`: gate `!is_openai()` at `provider.rs:369`; effect in `codex-api/src/endpoint/content_type_compat.rs` (module at `endpoint/mod.rs:1`), applied to the serialized body at `responses.rs:116-119`; commit `b4d4b12` = `b4d4b125cc` "feat: normalize content types for non-OpenAI providers (vLLM/SGLang)" — exists, subject and change-set match.
- `flatten_namespace_tools`: gate at `provider.rs:371`; `flatten_namespace_specs` at `codex-rs/tools/src/tool_spec.rs:158`; dotted names split back in the router via `ToolName::from_response_fields` (`core/src/tools/router.rs:254,289`; def at `protocol/src/tool_name.rs:54`) — "router splits dotted names back" accurate; commit `197ea16` = `197ea1642c` "feat: flatten namespace tools for non-OpenAI providers (vLLM/SGLang)" — exists, matches.
- `apply_patch_function_tool`: struct field at `provider.rs:59-63` (default `false` at :76), gate `!is_openai()` at `provider.rs:372`; status cell "IMPLEMENTED (items 1-3, 2026-09-16)" — all four item commits dated 2026-09-16 (verified via `git log --date=format`); accurate.
- "THESE are the ONLY supported divergence points" — the three seam flags are the only `!is_openai()`-gated wire capabilities; consistent with spec §3.4 "no new `is_openai()` branches".

**A3. §3 Components (function-tool design) — VERIFIED, two nits.**
- Capability: `apply_patch_function_tool: bool` in `ProviderCapabilities`, computed `!is_openai()` ✓ (A2).
- Spec: `create_apply_patch_function_tool(include_environment_id: bool) -> ToolSpec` at `apply_patch_spec.rs:79`; `ToolSpec::Function(ResponsesApiTool)` with `name: "apply_patch"`, `strict: false`, `patch: string` required (`Some(vec!["patch"])`), `environment_id: string` only when the flag is set ✓. The P1 argument-description constant is byte-identical to the spec §3.1 block (programmatic compare: code 2,198 chars = spec block 2,198 chars, `True`; tool-level description 126 = 126, `True`) — matching the spec §3.1 length claims (126 + 2,198). Two nits on this bullet: the quoted `description` paraphrase (n-D3) and the helper name (n-D4).
- Handler: `FunctionApplyPatchHandler` at `apply_patch.rs:480`; `tool_name()` → `ToolName::plain("apply_patch")`; `handle_call` matches `ToolPayload::Function { arguments }`, JSON-parses, missing `patch` → `FunctionCallError::RespondToModel` (:536 exact spec §3.3.3 string), non-string `patch` → `RespondToModel` (:543 exact spec §3.3.3 string), invalid JSON → `RespondToModel`; `environment_id` optional, injected as `*** Environment ID:` line via `with_environment_id_line`; delegates to the shared private `run_apply_patch_text` (`:400`; called by the custom handler at `:381` and the function handler at `:555`) — the "same shared execution path, no logic duplication" claim is exactly right, the name in the doc is wrong (n-D4). `matches_kind` → `ToolPayload::Function { .. }` (:607-608); `create_diff_consumer` → `None` (:611-612, v1 as documented); hooks emit `{"command": <patch>}` (`pre_tool_use_payload` :615-620, `post_tool_use_payload` :637-649) and `with_updated_hook_input` writes the updated patch back into the Function arguments JSON (:622-632) ✓.
- Registration: `spec_plan.rs:1257-1270` — at the `environment_mode.has_environment() && context.model_info.apply_patch_tool_type.is_some()` site, selects `FunctionApplyPatchHandler` when `capabilities().apply_patch_function_tool`, else `ApplyPatchHandler`; exactly one per session ✓.

**A4. §3 P1/P2/P3 format-remediation layer — VERIFIED (no over/under-claim).**
- P1: "the full patch format is taught in the function-tool `patch` argument description (non-OpenAI path only; `apply_patch_spec.rs`)" — the constant carries the full format (Begin/End markers, per-line prefixes, `++` encoding, one-hunk-per-file, multi-`@@`, empty-file rule, Example), byte-verified against spec §3.1; non-OpenAI-only via the capability gate (A3 registration) ✓. All 7 drift-guard substrings (spec §3.1 T2.1) present in the byte-verified text.
- P2: "Add-File leniency in the streaming parser: raw/empty/whitespace content lines accepted verbatim (defense in depth; provider-agnostic; `streaming_parser.rs`)" — the `AddFile` arm (`streaming_parser.rs:206-223`): structural lines keep meaning (handled first), `+`-prefixed → prefix-stripped content (unchanged), every other line appended verbatim via `contents.push_str(line); push('\n')` (empty/whitespace included); the old trailing `Err("not a valid hunk header")` is gone (arm ends `Ok(())`). Module doc carries the "Lenient add-file content (P2)" note (:3-9) as spec §3.2 requires ✓. Matches spec §3.2 exactly (including "verbatim is correct — trimming would corrupt indentation").
- P3: "teachable parse errors (bad hunk headers, Delete-File content, absent/non-string `patch`), including the `parse_patch` pre-pass boundary strings (P3.4; `streaming_parser.rs`, `parser.rs`), plus the P3.3 handler error split (`apply_patch.rs`)" — all four sites verified: P3.1 StartedPatch sentence extension (`streaming_parser.rs:201`, original sentence kept as exact prefix + spec §3.3.1 appended text byte-match); P3.2 DeleteFile replacement (`:228`, byte-match spec §3.3.2); P3.3 absent/non-string split (`apply_patch.rs:536/543`, byte-match spec §3.3.3); P3.4 pre-pass strings in `check_start_and_end_lines_strict` (`parser.rs:257-274`, Begin/End strings byte-match spec §3.3.4a/b). Pre-pass surface verified at all cited callers: function path `apply_patch.rs:411`, CLI `apply-patch/src/lib.rs:370`, shell-intercept `apply-patch/src/invocation.rs:116/123/170/175` — exactly the spec §3.3/§3.4 wiring. The parallel streaming boundary messages (`streaming_parser.rs:168/:184/:374` class) are the untouched class named by spec §3.4 ✓.
- Seam doc's framing matches spec §3's own definitions: P1 = primary fix (function-tool text), P2 = defense in depth, P3 = teachable errors; P1 scoped to non-OpenAI, P2/P3 provider-agnostic and rejected-inputs-only, P3.4 pre-pass-only — no over- or under-claim found.

**A5. §4 divergence row 7 — VERIFIED.** Present as row 7 (seam :193), 5 cells, matches the breakdown's required content element-for-element: provider-agnostic; surface = function-tool path + freeform diff consumer + `apply_patch` CLI (P2) + `parse_patch` pre-pass surface for P3/P3.4 (function path + CLI + shell-intercept); re-apply on upstream restructure of `streaming_parser.rs` or `parser.rs`; diff consumer's parallel streaming boundary messages intentionally unchanged (spec §3.4). Files column names the three carrier files (`apply-patch/src/streaming_parser.rs`, `apply-patch/src/parser.rs`, `apply_patch.rs` (P3.3)); all three are in the item-1/item-3 change-sets (`git show --stat e21f608ac4 939a6dc6f4`), and the "intentionally unchanged" claim is true at source (the three parallel message sites retain their pre-campaign text — verified by the item-3 evidence cross-check plus direct read of the current file).

**A6. §6 conflict sites + verification gate — VERIFIED.** Both required conflict-site bullets present with the exact required paths: `codex-rs/apply-patch/src/streaming_parser.rs` (seam :240-241, "P2 Add-File arm + P3.1/P3.2 error-message sites") and `codex-rs/apply-patch/src/parser.rs` (:242-243, "P3.4 `parse_patch` pre-pass boundary strings") — both files exist. Gate additions present: `just test -p codex-apply-patch` (:250-251) and the raw-format probe per spec §5.4 (:252-256) — the probe description matches spec §5.4 item 4 exactly (standalone `apply_patch` binary from the same release build, scratch dir, F1-shape raw Add-File patch without `+` prefixes / `# …` first content line, pass = exit 0 + byte-identical contents).

**A7. §6 invariants — VERIFIED.** The new bullet (seam :279-289) carries the spec §3.4 exact-invariant text in three clauses, verbatim except four documented trims: (1) "by this work" (implied by the enclosing "Invariants that must survive every rebase" header); (2) the `ApplyPatchArgumentDiffConsumer` explanatory parenthetical (surface trio intact: function-tool execution path, freeform diff consumer, `apply_patch` CLI); (3) the "v2 refinement:" tag (spec-versioning meta, nonsense in the seam doc); (4) "via invocation.rs:116/123/170/175" (line cites live in spec §3.3/§3.4, verified real at A4). Both §3.4 meta-sentences omitted deliberately, and both omissions are correct: the "seam doc §2 is stale" note is now false (item 5 IS the correction) and the one-time Azure-verification caveat is not a rebase invariant. First bullet (post M-A2) scopes to "Outbound request bytes for providers named `OpenAI`" with the Azure-named-→-function-tool pointer ✓.

## B. Internal consistency

**B1. Pinned evidence numbers — EXACT match to spec §1.2.** Seam doc §3 table: glm-5.2 `37 | 26 | ~70% (70.27%)` "as of the pinned snapshot" = spec row `glm-5.2 | 37 | 26 (~70%) | 11 | 0 | —` (pin 2026-09-15T23:34:04.678Z) ✓; qwen3.8-27b `413 | 5 | ~1% (1.21%)` = spec row (pin 2026-09-15T23:30:11.662Z) ✓; "glm-5.2 on 09-15 alone: 13 of 21 calls rejected ≈ 62%" = spec §1.2 "Most recent day (09-15) alone: 13 of 21 glm calls rejected (~62%)" ✓ (13/21 = 61.9%). The two rejection classes (F1/F2 raw content, F4 missing boundary markers) match spec TL;DR (19 of 26 / 7 of 26 — seam omits the split, summary-level, no contradiction).

**B2. ≈58× — math verified, one wrong equation in the evidence doc (M-D1).** 26/37 = 70.27% ✓; 5/413 = 1.21% ✓; exact ratio (26/37)/(5/413) = 10738/185 = 58.0432; rounded-division 70.27/1.21 = 58.0744. Both round to ≈58×; the spec Conclusion (:213) states "≈58× qwen's ~1% (26/37 = 70.27% vs 5/413 = 1.21%)" — correct. The evidence doc's "70.27 / 1.21 = 58.04" (evidence :219, :502) is the false equation (see M-D1).

**B3. Line-number anchors — verified.** The seam doc carries exactly two explicit line anchors; both are real: `model-provider-info/src/lib.rs:546-547` and `:40` (A1). All other code references are symbol/file level; 10 spot-checked, 9 exact, 1 name mismatch (n-D4): `create_apply_patch_function_tool` (apply_patch_spec.rs:79) ✓, `FunctionApplyPatchHandler` (apply_patch.rs:480) ✓, `flatten_namespace_specs` (tool_spec.rs:158) ✓, `check_start_and_end_lines_strict` (parser.rs:257) ✓, `run_apply_patch_text` (apply_patch.rs:400) [doc says `run_patch_text` — n-D4], `content_type_compat` (endpoint/mod.rs:1) ✓, spec_plan registration site (spec_plan.rs:1257) ✓, `apply_patch_tool_type: Some(Freeform)` fallback (model_info.rs:140, divergence row 4) ✓, `from_response_fields` (tool_name.rs:54 / router.rs:254,289) ✓, P3.4 callers (apply_patch.rs:411, lib.rs:370, invocation.rs:116/123/170/175) ✓.

## C. Spec-delta verification

**C1. Status line (spec :7-14) — VERIFIED.** `Status: IMPLEMENTED — all five items landed on branch feat/normalize-content-types-vllm` with item 1 / P2 `e21f608ac4`, item 2 / P1 `a4d5af1f62`, item 3 / P3 `939a6dc6f4`, item 4 / T4 `a90b15d600`, item 5 `<<ITEM5-SHA>>` (placeholder, "coordinator fills after commit"). All four real SHAs exist and map to the correct items (subjects: "P2 Add-File leniency (item 1)", "P1 function-tool spec text (item 2)", "P3 teachable errors (item 3)", "T4 integration tests on renamed non-OpenAI provider (item 4)"; all dated 2026-09-16; `git show --stat` change-sets match the evidence's "Item commit facts"). The placeholder is the contracted form for the still-uncommitted item-5 commit (breakdown §8 row 5 is the matching "—" placeholder; evidence D5 records the decision). "Spec review loop terminated at round 5 … (breakdown v3)" — corroborated: r5 seat reports `apply-patch-format-spec-r5-seatI.md` / `r5-seatJ.md` both APPROVED 0B/0M (1m/3n; 1m/1n), matching the §7 Round-5 entry; breakdown §9 Round 2 = 0B+0M (seats M/N), fixes applied as v3.

**C2. §1.2 Conclusion — VERIFIED.** :213 "≈58× qwen's ~1% (26/37 = 70.27% vs 5/413 = 1.21%)" — the single in-scope occurrence; `grep "≈60×"` over the spec returns exactly one line, :1022, the §7 R5-map I-N1 historical row (left untouched per contract).

**C3. Everything else untouched — VERIFIED (spec is untracked; full-file diff vs the v4 archive enumerates every byte difference).** `diff docs/reviews/apply-patch-format-spec-v4-draft.md docs/responses-compat-apply-patch-format.md` = exactly 8 hunks (`diff -u`), each classified:
1. Header: Evidence line gains "· v4 archive: …" (v5 bookkeeping, matches the per-version archive-pointer convention seen in v0–v3) + status block v4-SPEC → IMPLEMENTED (item 5).
2. §1.2 VERIFY definition gains `Failed to find context` (file_update.rs:110) — R5 map I-N3/J-M1.
3. §1.2 glm breakdown "both patches end with … v5: corrected from 'one'" — R5 map I-M1.
4. §1.2 Conclusion: "an order of magnitude above" → ≈58× — R5 map I-N1 (v5) + item-5's ≈60×→≈58× (K-N1/breakdown row 13); net single-location change.
5. T4.2 scaffold cite `:933-942` → `:933-943` — R5 map J-N1 (first cite).
6. §7 R2-map D-M2 row: same cite + "v5: constructor range end :942→:943" — R5 map J-N1 (second cite, "in both cites").
7. §7 R3-map E-N1 row opening restated — R5 map I-N2.
8. §7: "Round 5: pending" placeholder replaced with the completed-round record + the "Round-5 resolution map (v4 → v5)" section + "Spec review stage: COMPLETE" bullet — v5 finalization bookkeeping (pre-item-5: the entry ends at "Next: stage 3 (task breakdown)" and mentions no item-5 work).
No hunk touches the TL;DR, §2, §3.1–§3.4, §4, §5, §6, or any round-1..4 map; heading structure identical to v4 plus the one new §7 subsection. Spec mtime 2026-09-16 14:48:39 predates both r1 reports (19:41/19:44) and the r1 fixes (19:58) — the spec has not moved since item 5's own edits.

## D. Evidence doc (`docs/reviews/impl-item5-tdd-evidence.md`)

**D1. Worker attestation vs the recorded gate logs — CONSISTENT (one false equation, M-D1).** Attestation "Gates: 1 ✓ (115/115), 2 ✓ … (0 deterministic failures; binding check empty), 3 — capability-gate target green, 2 pre-existing environmental TLS failures recorded (D2), 4 ✓ (hazard triggered + restored, D3), 5 ✓" matches the five gate sections one-for-one, and each gate section matches the raw log:
- Gate 1 (`/tmp/item5-gate1.log`): `Summary [11.821s] 115 tests run: 115 passed, 0 skipped`, `GATE1_EXIT=0` — verbatim ✓.
- Gate 2 (`/tmp/item5-gate2.log`): summary line verbatim ✓; binding grep re-run by this seat over the full log: `grep -E "thread .+ panicked" | grep -v 'lib.rs:388' | sort -u` → **0 lines** ✓; panic census re-counted: **92 panics, 92/92 at `core/tests/common/lib.rs:388`** ✓ (matches the evidence's census); per-target nextest-line population: **exactly 40 lib-target tests, all `PASS`, zero lib retry/FAIL lines** and 118 = 40 lib + 78 suite — independently confirms the corrected "All 40 `codex-core` lib unit tests … passed on first try" (evidence :355), including the named P1 spec tests (`tools::handlers::apply_patch_spec::tests::*`) and both P3 teachable-error handler tests (`function_apply_patch_rejects_{missing,non_string}_patch_argument_with_teachable_error`); the single FAIL identity and its lib.rs:388 panic match.
- Gate 3 (`/tmp/item5-gate3.log`): summary line verbatim ✓; capability-gate target `configured_provider_apply_patch_function_tool_matches_provider_support` PASS 0.184s first try ✓; exactly 2 FAILED = the two named `amazon_bedrock` TLS tests, FAIL on both attempts ✓; "no valid root certificates parsed" appears 4× (= 2 tests × 2 attempts), matching D2's "both attempts, both tests" ✓.
- Gate 4/5 quotes match `/tmp/item5-gate4.log` ("Fixed core/tests/suite/openai_file_mcp.rs (1 fix)", exit 0) and `/tmp/item5-gate5.log` (0 bytes = silent success) ✓.

**D2. "2 pre-existing environmental TLS failures" — EVIDENCED, not asserted.** D2 gives three independent grounds: (a) item 5 changed zero tracked files (F below); (b) the panic is third-party `aws-smithy-http-client-1.1.12` rustls trust-store init ("TrustStore configured to enable native roots but no valid root certificates parsed!", `rustls_provider.rs:116`) on the AWS-SDK `provider.auth()` path — the panic text is present in the raw log; (c) the only working-tree `amazon_bedrock/mod.rs` delta is one `apply_patch_function_tool: false` capability field plus two matching test literals — verified: nowhere near auth/TLS. Both r1 seats independently reproduced the identical two-test/panic class in their own gate-3 re-runs. Meets the checklist's "described with evidence" bar.

**D3. Gate coverage vs breakdown gate list — COMPLETE.** Breakdown Item 5 step 3 requires: `just test -p codex-apply-patch` (gate 1 ✓), `just test -p codex-core apply_patch` (gate 2 ✓), `just test -p codex-model-provider` (gate 3 ✓), `just fix -p` over touched crates (gate 4: `-p codex-apply-patch -p codex-core -p codex-model-provider` ✓), `just fmt` (gate 5 ✓). No `--all-features`, no full `just test` (per the "no full just test without asking" clause) ✓.

**D4. Pre-check facts — VERIFIED.** `:40`/`:546-547` single-hit greps (A1); item commit facts incl. "all four commits dated 2026-09-16" (verified); the ≈60× pre-edit census (":209 Conclusion + :1018 §7 I-N1, the only two occurrences") is consistent with the v4-archive diff (hunks 4 and 8). The pre-edit anchors corrected per m-F2 (Components :79, Testing :121, Out of scope :131) now match seat B's reconstruction.

**D5. Tree-state staleness (M-D2).** See findings.

## E. r1 cross-check (per finding, verified against current artifact text)

| ID (log) | Log claim | Independent verification | Status |
|---|---|---|---|
| M-A1 (seat A A-1, Major) | FIXED — Decision paragraph reworded, scoped to providers named `OpenAI`, §3.4 invariant inline | seam :81-86 now reads "OpenAI/Azure providers are untouched … — scoped to providers **named `OpenAI`** (spec §3.4 invariant: … Azure-*named* providers take the function tool, as pinned by the capability test)" — matches the log's description | **FIXED confirmed** (residual leading-clause style: n-D5) |
| M-A2 (seat A A-2, Major) | FIXED — invariants bullet 1 re-scoped to "Outbound request bytes for providers named `OpenAI` …" | seam :274-277 is exactly the log's quoted text; the old "OpenAI/Azure provider behavior is byte-identical" wording is gone | **FIXED confirmed** |
| m-F1 (seat B F1, minor) | FIXED — "All 46 … lib unit tests" → "All 40" | evidence :355 says 40; raw log population = exactly 40 lib tests, all PASS first-try (D1) | **FIXED confirmed, count re-verified against raw log** |
| m-F2 (seat B F2, minor) | FIXED — pre-edit anchors Components :76→:79, Testing :130→:121, Out of scope :141→:131 | evidence pre-check now lists :79/:121/:131, matching the log and seat B's reconstruction | **FIXED confirmed** |
| n-F3 (seat B F3, nit) | RECORDED — trailing blank line normalized to single trailing newline, no re-add | spec file ends `…zero Major.\n` — single trailing newline, no trailing blank line | **RECORDED confirmed** |
| n-F4 (seat B F4, nit) | RECORDED — 10738/185 = 58.0432 supports ≈58× and the "58.04" phrasing; rounded division 58.07 also rounds to ≈58× | arithmetic re-derived: 58.0432 and 58.0744 ✓; shipped ≈58× stands. But the evidence doc's own "70.27 / 1.21 = 58.04" equation was left in place (evidence :219, :502) and is false as written | **RECORDED confirmed; residual equation → M-D1** |
| n-F5 (seat B F5, nit) | RECORDED — spec §3.4 historical "stale" narrative left untouched per contract | spec :608-610 still carries "the seam doc §2 'plus an Azure branch' description is stale and is corrected in the same PR set (v1, seat B M5 / seat A M2)" — untouched, as the contract requires | **RECORDED confirmed** |
| N-1 (seat A, nit) | FIXED — P3 bullet names all three P3 carrier files | seam P3 bullet now ends "plus the P3.3 handler error split (`apply_patch.rs`)" | **FIXED confirmed** |
| N-2 (seat A, nit) | ACCEPTED — §6 mixed path spelling follows locked spec spelling (D1) | seam :240-243 still use the `codex-rs/`-prefixed spelling; siblings omit the prefix — unchanged, as accepted | **ACCEPTED confirmed** |
| N-3 (seat A, nit) | ACCEPTED — gate bullet keeps `just test -p codex-apply-patch` | seam :250-251 unchanged | **ACCEPTED confirmed** |
| N-4 (seat A, nit) | ACCEPTED — invariant bullet is a faithful condensation (four small justified trims) | the four trims are exactly those listed in A7; nothing load-bearing dropped | **ACCEPTED confirmed** |
| N-5 (seat A, nit) | FIXED — status cell "SPECIFIED BELOW" → "IMPLEMENTED (items 1-3, 2026-09-16)" | seam :51 status cell reads exactly that; `grep "SPECIFIED BELOW"` over the seam doc: zero hits | **FIXED confirmed** |

**No mislabeled finding.** Every log claim (FIXED/RECORDED/ACCEPTED) matches the artifact text. All r1 line cites (A-1 :81, A-2 :269-270, N-5 :51, etc.) refer to the pre-fix 287-line file; post-fix positions shifted +4/+5 — expected from the M-A1/M-A2 rewordings, not a mislabel.

## F. Docs-only verification

- `git status --short`: **1364 entries, all `??` (untracked); zero `M`/`D`/`A`/`R` entries.** No tracked file is modified or deleted — inside or outside docs/.
- `git diff --stat` and `git diff --cached --stat`: empty.
- Untracked set: `docs/responses-compat-apply-patch-format.md`, `docs/responses-compat-seam.md`, `docs/reviews/*` (campaign review tree incl. this file's siblings), `docs/superpowers/`, `docs/vllm-glm-toolcall-research.md`, `engine-redesign-charter.md`, and 1300+ `codex-rs/vendor/**` dirs (cargo-vendor build artifacts — ignored per task).
- The 12-file/809-insertions/20-deletions working-tree delta the evidence doc's "Final state" describes was committed as `799ec98053` ("commit working-tree function-tool seam baseline", operator-directed, 2026-09-16 19:58:59 — 14 s after the evidence doc's last write). That is a distinct baseline commit, not an item-5 product-code change; item-5's own contribution in the tree is docs-only, consistent with the DoD "one commit (docs + lockfile if any)" still pending (the `<<ITEM5-SHA>>` fill). No `Cargo.toml`/`Cargo.lock`/`MODULE.bazel.lock` changes anywhere in the item-5 change set.

## Findings

**M-D1 [Minor] — evidence doc states a false arithmetic equation twice.**
`docs/reviews/impl-item5-tdd-evidence.md:219` ("Factual verification"): "**≈58×** — 70.27 / 1.21 = 58.04 (arithmetic check)" and `:502` ("Worker attestation"): "arithmetic-verified (70.27/1.21 = 58.04)". 70.27 / 1.21 = **58.0744**, not 58.04; 58.04 is the *exact* ratio (26/37)/(5/413) = 10738/185 = 58.0432 — which the same doc's n-F4 resolution line states correctly. A reader re-running the section's advertised "arithmetic check" gets 58.07 and a mismatch against the stated 58.04. The shipped ≈58× is correct under both computations and no other artifact carries the false equation (spec :213 cites the two percentages, not a quotient; the §7 I-N1 row says "≈ 58×"). r1 rated this a nit and the log records "RECORDED, shipped value stands"; this seat upgrades it to minor for consistency with m-F1's precedent (factual inaccuracy inside the evidence artifact — the wrong count 46→40 was a minor) and because the equation is false rather than merely imprecise. Fix: replace both with "exact ratio (26/37)/(5/413) = 10738/185 = 58.04× (70.27/1.21 = 58.07 under rounded percentages)". No verdict input affected.

**M-D2 [Minor] — evidence doc's tree-state claims are stale vs current HEAD; the baseline commit is unmentioned.**
Header: "working-tree HEAD `3db1b1381a` … the campaign function-tool seam remains uncommitted working-tree state per breakdown §0"; "Final state": "the ONLY tracked files modified in the tree are the pre-existing campaign-seam files — 12 total … `git diff HEAD --stat` … 809 insertions / 20 deletions"; attestation: "no commits made (tree left uncommitted per the task contract)". At writing time (19:58:45, doc mtime) all three were true; 14 seconds later the operator-directed baseline commit `799ec98053` committed exactly those 12 files, so the current tree has **zero** tracked modifications (`git diff HEAD --stat` empty — verified this seat) and the seam doc is 294 lines, not the recorded 287 (r1 fixes). No section of the evidence doc or r1 log mentions `799ec98053`. The doc is a point-in-time record (its header pins its HEAD, and the baseline commit message explains the transition), which is why this is minor rather than major — but the present-tense "Final state" block is the part a rebase auditor would cross-check against HEAD, and it now contradicts it until the reader finds the commit message. Fix: one addendum line, e.g. "Superseded at 2026-09-16 19:58 EDT: operator-directed baseline commit `799ec98053` committed the 12-file seam (this record describes the pre-commit tree); post-r1 seam doc is 294 lines."

**n-D3 [Nit] — seam doc Components description bullet quotes text that is not in the code or spec.**
`docs/responses-compat-seam.md:98-99`: "`description`: same guidance as the freeform spec, plus an explicit 'put the ENTIRE patch text in `patch`; do not wrap in JSON or markdown'". That phrase occurs nowhere in the codebase or the spec (repo-wide grep: the only hits are seam :99 and the item-2 r1 seat-B report quoting it). The actual tool-level description is "The `apply_patch` tool can be used to edit files (add, delete, update, move). The complete patch goes in the `patch` argument." (byte-verified, C/A3; exact text pinned in spec §3.1). The substance is correct (both tool descriptions carry the edit-files guidance + anti-wrapping rule, and the P1 argument description carries the "ENTIRE patch" instruction); only the quoted wording is a composite. Pre-existing Components text (not in item 5's change list), so optional cleanup.

**n-D4 [Nit] — seam doc names the shared execution helper `run_patch_text(...)`; the actual function is `run_apply_patch_text`.**
`docs/responses-compat-seam.md:112-116`: "extract the post-parse body of the existing `handle_call` into a private `run_patch_text(...)` used by both handlers". The real private function is `run_apply_patch_text` (`core/src/tools/handlers/apply_patch.rs:400`; called by the custom handler at `:381` and the function handler at `:555`). The described structure (shared post-parse body, no logic duplication, both handlers delegate) is exactly right — only the identifier is wrong, and a grep for the doc's name finds nothing. Pre-existing Components text; optional cleanup.

**n-D5 [Nit] — M-A1-fixed Decision sentence still opens with the false clause "OpenAI/Azure providers are untouched".**
`docs/responses-compat-seam.md:81-82`: "OpenAI/Azure providers are untouched: they keep the native custom+grammar path — scoped to providers **named `OpenAI`** (…)". The sentence as a whole is now accurate (the dash clause rescopes and states the §3.4 invariant, per the M-A1 resolution — FIXED confirmed in §E), but a reader stopping at the first clause still reads a false statement, since Azure-*named* providers take the function tool. Optional tightening: "OpenAI-*named* providers are untouched: they keep the native custom+grammar path (…)".

## Verdict

Item 5's deliverable is sound. Every seam-doc addition required by the breakdown is present, source-verified, and spec-conformant (A1–A7); the three pinned evidence numbers match spec §1.2 exactly and the ≈58× math holds (B); the spec's item-5 footprint is precisely the status line plus the ≈58× token, with the rest of the file byte-accountable to the v4 archive plus the five recorded v5 fixes (C); the evidence doc's gate numbers reproduce from the raw logs, including the independently re-counted 40-lib-test population and the empty binding grep, and the TLS failures carry real evidence (D); all twelve r1 findings' log claims match the artifact text with no mislabels (E); and the tree is docs-only with no tracked modification or deletion anywhere (F). The two minors are both evidence-doc hygiene (a false quotient stated twice; a stale tree-state block with the intervening baseline commit unmentioned) and the three nits are pre-existing or residual wording items — none touches the spec, the invariant text, the pinned numbers, or any verdict input.

**Counts: 0 Blocking, 0 Major, 2 Minor, 3 Nit.**

SEAT D round 2: APPROVED — 0 Blocking, 0 Major, 2 Minor, 3 Nit
